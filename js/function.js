renderMonths(target, data) {
    let MonthHTML = '';
    for ( let i=0; i<data.length ; i++) {
        MonthHTML += `<div class="table-content">
                    <div class="months-name"${data[i].months}>`
};
}...
